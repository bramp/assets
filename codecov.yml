coverage:
  status:
    project:
      default:
        target: 0.85
        threshold: 0.02
    patch:
      default:
        target: 0.90
        threshold: 0.02

comment:
  layout: "reach,diff,flags,files"
  require_changes: false
