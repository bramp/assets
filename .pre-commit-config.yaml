repos:
  - repo: local
    hooks:
      - id: make-analyze
        name: make analyze (golangci-lint, go vet, staticcheck)
        entry: make analyze
        language: system
        pass_filenames: false
        stages: [pre-commit]
